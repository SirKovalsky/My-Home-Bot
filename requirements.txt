# Для современного Python (3.10+). Для DSM 6.2 / Python 3.8 используйте
# requirements-dsm6.txt — см. README, раздел про установку на Xpenology.
aiogram>=3.7,<4
# requests + PySocks: PySocks обязателен для схем socks5h:// в параметре proxies.
requests[socks]>=2.31
# Обёртка Transmission RPC (работает через requests, используем БЕЗ прокси).
transmission-rpc>=7.0.0
python-dotenv>=1.0.0
# Обход Cloudflare на трекерах: подделка браузерного TLS/HTTP-фингерпринта.
curl_cffi>=0.9
